import 'package:flutter/cupertino.dart';

class TestMinigamePage extends StatelessWidget {
  const TestMinigamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("test")
    );
  }
}